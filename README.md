# \# 📌 CRUD de Tarefas em Spring Boot

# 

Este projeto é um exemplo prático de um \*\*CRUD (Create, Read, Update, Delete)\*\* desenvolvido com \*\*Spring Boot\*\* e \*\*Spring Data JPA\*\*, utilizando banco de dados em memória \*\*H2\*\* para persistência.







# \## 🚀 Tecnologias utilizadas

 \*\*Java 24\*\*

 \- \*\*Spring Boot 4.0.1\*\*

 - \*\*Spring Data JPA\*\*
 - \*\*Hibernate\*\*
 - \*\*H2 Database\*\*
 - \*\*Maven\*\*

 

 ---

 

\## 📂 Estrutura do projeto

\- `model/` → classes de entidade (`Tarefa`)

\- `repository/` → interfaces que estendem `JpaRepository`

\- `controller/` → endpoints REST

\- `resources/application.properties` → configuração do banco e do Spring Boot

# 

# ---

# 

## 📌 Endpoints da API

- `POST /tarefas` → cria uma nova tarefa  

- `GET /tarefas` → lista todas as tarefas  

- `GET /tarefas/{id}` → busca tarefa por ID  

- `PUT /tarefas/{id}` → atualiza uma tarefa existente  

- `DELETE /tarefas/{id}` → remove uma tarefa  

 

 





# \. Clone este repositório:
 &nbsp;  git clone https://github.com/Jonas-Honorato/springboot-crud-tarefas.git



